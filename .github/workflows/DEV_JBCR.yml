name: Ejemplo de Action

on:
  push:
    branches:
      - DEV

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Imprimir mensaje
      run: echo "¡Hola, este es un ejemplo de GitHub Actions desde JBCR!"
