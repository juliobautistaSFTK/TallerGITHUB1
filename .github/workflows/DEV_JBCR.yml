name: Ejemplo de Action

on:
  workflow_dispatch:  # Disparador manual

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Imprimir mensaje
      run: echo "¡Hola, este es un ejemplo de GitHub Actions!"
